[pytest]
# Home Assistant harness suite only. The dependency-free tests/ suite is run
# separately via `python -m unittest discover -s tests -t .` and is intentionally
# not collected here (it stubs `homeassistant`, which would clash with the real
# package the harness suite loads).
testpaths = tests_ha
asyncio_mode = auto
pythonpath = .
