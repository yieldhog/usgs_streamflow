# Test dependencies for the Home Assistant harness suite (tests_ha/).
# The dependency-free logic suite (tests/) needs none of these — it runs under
# plain `python -m unittest`.
#
# pytest-homeassistant-custom-component pins a compatible Home Assistant + pytest
# stack; let it resolve the versions.
pytest-homeassistant-custom-component
